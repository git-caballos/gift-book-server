/**
 * 前端运行时配置文件
 * API_BASE_URL：后端（API）地址与端口，部署时手动修改，保存后刷新页面生效。
 * 后端托管前端：留空 ""，请求自动走后端托管的 API 地址（推荐）。
 * 填写格式："http://主机:端口"，例如：http://localhost:8080
 */
window.__APP_CONFIG__ = {
  API_BASE_URL: ""
};
